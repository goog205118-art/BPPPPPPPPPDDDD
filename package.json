{
  "name": "resource-workbench",
  "version": "0.1.0",
  "private": true,
  "description": "Local workbench for creator and resource management.",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check \"api/[...route].js\" && node --check app/app.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0",
    "exceljs": "^4.4.0"
  }
}
