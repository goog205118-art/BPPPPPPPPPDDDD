{
  "name": "resource-workbench",
  "version": "0.1.0",
  "private": true,
  "description": "Local workbench for creator and resource management.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node tools/local-server.cjs",
    "dev": "node tools/local-server.cjs",
    "check": "node --check tools/local-server.cjs && node --check \"api/[...route].mjs\" && node --check app/app.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0",
    "exceljs": "^4.4.0"
  }
}
