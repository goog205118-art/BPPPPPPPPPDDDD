# 当前状态

## 更新时间

2026-08-18

## 当前结论

第一版 MVP 已完成本地落地测试，可以作为“达人与资源沉淀工作台”的可用原型继续试用。

线上 Vercel 版本的部署文件已准备完成：资料和 AI 设置会改存 Vercel Blob，网页由访问密码保护，不再依赖本地端口、VPN、Python 或 SQLite。

## 已完成

- 本地服务已可启动并访问 `http://localhost:4173/`
- 五个核心模块已可用：达人库、待开发达人、资源库、合作记录、本周资源匹配
- 已支持搜索、筛选、新增、编辑、删除
- 已支持 CSV / XLSX 表格导入
- 已支持导入预览和字段映射
- 已支持 JSON 导入 / 导出
- 已支持 CSV 导出
- 主存储已切换为 SQLite
- 已增加 JSON 镜像文件，方便备份和排查
- 中文界面、中文数据、CSV 导出、XLSX 导入已通过 UTF-8 验证
- 第一版接口回归已通过：新增、编辑、删除、导出、导入、数据恢复
- 已修复导入按钮形态，顶部 JSON 导入和表格导入按钮尺寸保持一致
- 已将左侧录入表单改为紧凑双列，减少新增 / 编辑时的向下翻页
- 已支持 Excel 多 Sheet 导入，导入预览会显示来源工作表数量
- 已将智能建议升级为可应用建议，可按当前表单内容补充标签、分级、长期合作意向和复盘结论
- 已增加导入影响预判，可在确认导入前看到预计新增、预计更新和文件内重复数量
- 确认导入时会跳过同一文件内重复行，降低批量导入污染数据的风险
- 已增加导入历史和一键回滚，批量导入后可恢复到导入前快照
- 已增加疑似重复识别与合并面板，可在当前模块内处理重复达人、资源或合作记录
- 已完善达人录入字段，新增社媒地址和达人邮箱
- 已新增待开发达人页：粘贴社媒地址后可调用独立的快速 AI 配置，自动整理核心资料
- 已新增两套前台 AI 参数设置：达人完整补全与待开发快速录入可分别设置 Gemini 或 OpenAI 兼容协议、地址、Key、模型和代理
- 已将 AI 达人链接补全入口集成到达人“社媒地址”行，补全结果只填空字段，需人工复核后保存
- 达人库和待开发达人会按社媒地址与邮箱双向查重；导入也会跳过跨库重复资料
- 待开发达人可确认后直接转入达人库，原线索会保留并标记“已转达人库”
- 已新增本周资源匹配：按国家、品类、新品/清库存、预算、独家条件和上线周期排序资源，可保存待联系清单和执行结果
- 已统一国家名称，导入和保存时会将 United States / USA 等常见写法归为“美国”，筛选项会按实际数据动态生成
- 合作记录已增加达人 ID、资源 ID 和匹配任务 ID 的稳定关联，历史名称字段仍保留
- 已增加项目内 `data/storage` 存储目录和双击启动文件 `启动资源工作台.cmd`
- AI 设置页支持 Gemini 和 OpenAI 兼容协议；完整补全与待开发快速录入可分别选择地址、Key、模型和代理，也可选择本地保存 Key 或从环境变量读取
- 已新增 Vercel Serverless API、Vercel Blob 云端存储、线上访问密码入口、GitHub Actions 自动部署工作流与线上部署说明

## 当前存储

- SQLite 数据库：`%TEMP%\ResourceWorkbench\resource-workbench.sqlite3`
- JSON 镜像：`%TEMP%\ResourceWorkbench\state.json`
- AI 设置：随 SQLite 数据库保存；旧版 `data/storage\ai-settings.json` 会自动迁移
- 项目种子数据：`D:\编码\资源复用\resource-workbench\data\state.json`

## 当前数据量

数据以运行中的 SQLite 数据库为准。项目内 `data/state.json` 与 `data/storage/state.json` 曾存在历史镜像不同步，示例数据清理前不再在本文档中写死数量，避免误导。

## 已知边界

- Gemini AI 代理已接入，真实补全需要先在“设置”页配置 API Key，也可改用 `GEMINI_API_KEY` 或 `GOOGLE_API_KEY` 环境变量
- 重复合并已有基础规则，后续需要更细的去重策略
- 当前为可双击启动的本地 Web 程序；若需要完全无命令窗口的桌面图标，可在下一阶段封装桌面壳
- 线上版面向单一负责人或小型可信团队，暂不适合多人同时编辑同一份资料

## 下一步

1. 用真实业务目标完成至少一轮本周资源匹配与结果回流
2. 增加匹配任务到合作记录的直接关联和复投汇总
3. 做桌面壳封装，隐藏服务窗口
4. 增加更完整的数据备份、恢复点和版本迁移提示
