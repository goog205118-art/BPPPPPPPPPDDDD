# 工作台 UX 重构执行台账

## 文档职责

本台账是“资源库工作台信息架构与桌面 UX 重构”的唯一执行入口。它独立于已完成的 `CRM_EXECUTION_TASKS.md`：不重复、替换或改写 CRM 能力，只调整用户看到的入口、层级、默认视图和操作密度。

- 建立日期：2026-09-13
- 当前里程碑：信息架构与日常工作入口重构
- 数据原则：不读取或修改正式业务资料；不连接真实 IMAP、SMTP、AI 或线上存储进行视觉验证。
- 交互原则：保留现有人工确认、AI 审核、品牌隔离、邮件安全与保留控制；界面改造不得弱化这些边界。

## 状态与记录规则

| 状态 | 含义 |
| --- | --- |
| `planned` | 需求已登记，尚未进入实现。 |
| `active` | 正在实现或验证。任何时刻只允许一个核心任务处于此状态。 |
| `blocked` | 有外部阻塞，须记录恢复条件。 |
| `done` | 已通过对应验收和回归验证，并记录提交号。 |
| `deferred` | 已确认推迟，须写明重新评估条件。 |

1. 实现前先将最小可验证工作项设为 `active`，并追加阶段日志。
2. 每个小阶段结束后必须追加日志，记录文件、验证、提交和下一门槛。
3. 功能提交与台账检查点提交分开，检查点提交引用功能提交。
4. 上下文恢复时，先读取“当前执行指针”和最后三条日志；不得凭聊天历史重新规划或重复已完成步骤。
5. 所有行为改动后至少运行 `npm.cmd run check`；影响既有 CRM 页面时，补充相关专项回归。

## 当前执行指针

| 项目 | 当前值 |
| --- | --- |
| 当前任务 | `UX-30-01` |
| 当前状态 | `active` |
| 当前目标 | 在合作跟进中新增默认行动列表，保留原看板作为可切换的全漏斗视图。 |
| 已完成前序 | `UX-00-01` 基线与执行治理；`UX-10` 设置页信息架构；CRM 固定执行台账全部任务。 |
| 禁止提前启动 | 不改变邮件/AI/存储业务契约；不自动发送、自动推进高风险阶段或更改正式数据。 |
| 最近已验证基线 | 隔离存储、访问密码 `ux-baseline`、端口 `4191` 的桌面基线已打开；未读取真实资料或连接外部服务。 |

## 固定任务表

### UX-00：基线与执行治理

| ID | 优先级 | 状态 | 工作项 | 前置 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| `UX-00-01` | P1 | `done` | 建立独立 UX 台账，盘点现有顶部栏、导航、设置页、合作跟进、今日推进和达人库展示，并定义视觉/回归基线。 | 无 | 已记录当前结构、影响范围、验证路径；后续工作只从此台账恢复。 |

### UX-10：设置页信息架构

| ID | 优先级 | 状态 | 工作项 | 前置 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| `UX-10-01` | P1 | `done` | 将设置页拆为左侧二级导航与独立内容区：工作区与品牌、AI 与自动化、官方邮箱、邮件安全与留存、数据导入导出、偏好设置。 | `UX-00-01` | 进入任一设置项时只显示相关配置；顶部显示明确状态；保存行为和现有字段兼容。 |
| `UX-10-02` | P1 | `done` | 将官方邮箱配置改为清晰的分步引导：品牌/账户、IMAP 与文件夹、SMTP 发信、完成与状态。 | `UX-10-01` | 用户可在不滚动整页的情况下完成配置、测试和状态确认；不改变真实邮件连接边界。 |
| `UX-10-03` | P2 | `done` | 精简 AI 与自动化的默认内容，将 API、协议、代理等低频配置放入高级区；将时区和录入候选项归入偏好设置。 | `UX-10-01` | 默认视图可确认默认模型、功能分配与风险策略；高级字段按需展开。 |

### UX-20：日常入口、导航与顶部工具栏

| ID | 优先级 | 状态 | 工作项 | 前置 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| `UX-20-01` | P1 | `done` | 有业务数据时默认进入今日推进；无数据时显示导入表格、新增达人、创建品牌三个明确起点。 | `UX-00-01` | 首页不再默认落在空达人表；新旧用户均有明确下一步。 |
| `UX-20-02` | P1 | `done` | 将主导航按资料、协作、分析分组，突出今日推进和合作跟进。 | `UX-20-01` | 八个业务入口不再平铺；现有页面均可到达且激活状态准确。 |
| `UX-20-03` | P2 | `done` | 收拢顶部工具栏：常驻搜索、工作区、今日数量、设置；刷新/JSON/品牌管理/时区管理进入更多菜单；常驻两个时区、其余展开查看。 | `UX-20-02` | 首屏顶部更轻；所有原有操作仍有可发现的入口。 |

### UX-30：合作跟进行动视图

| ID | 优先级 | 状态 | 工作项 | 前置 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| `UX-30-01` | P1 | `planned` | 在合作跟进中新增默认行动列表：达人、阶段、最新邮件、下一步、截止时间和主要操作；原看板可切换。 | `UX-00-01` | 日常跟进不依赖横向滚动看板；现有 Case/任务/邮件入口保留。 |
| `UX-30-02` | P2 | `planned` | 看板按沟通、履约、内容三组呈现或折叠，保留全漏斗视图。 | `UX-30-01` | 七阶段信息可一眼理解，窄屏不因列数失去可用性。 |

### UX-40：今日推进与达人库信息密度

| ID | 优先级 | 状态 | 工作项 | 前置 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| `UX-40-01` | P1 | `planned` | 精简今日推进默认筛选与任务提示，突出逾期、今天到期、等待回复；每条任务给出原因和主操作；补齐可行动空状态。 | `UX-20-01` | 打开即能判断优先级和下一步；筛选仍可展开使用。 |
| `UX-40-02` | P2 | `planned` | 精简达人库默认列为达人、平台、品牌、邮箱、状态/阶段、优先级、最近联系、下一步；扩展字段保留在列设置和详情抽屉。 | `UX-00-01` | 表格首屏更易扫描，不丢失既有字段或编辑能力。 |

### UX-50：视觉 QA 与发布核验

| ID | 优先级 | 状态 | 工作项 | 前置 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| `UX-50-01` | P1 | `planned` | 针对桌面与窄视口检查深色界面对比、布局层级、文本溢出、弹层、导航和关键空状态，并运行全量适用回归。 | `UX-10` 至 `UX-40` | 所有任务均有行为和视觉证据；不引入白底、不可读文字或关键入口丢失。 |

## 本阶段不启动

1. 任何 CRM 数据模型、邮件同步、AI 提示词、发送或自动阶段推进的业务重写。
2. 更改官方邮箱的真实连接、授权密码、历史邮件、正文或附件数据。
3. 未经用户确认的品牌资料、正式数据导入/导出或线上部署操作。

## 阶段变更日志

> 只追加，不改写历史记录。

| 时间 | 任务 ID | 状态变化 | 改动文件 | 验证/证据 | 提交 | 结论与下一门槛 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-13（America/New_York） | `UX-00-01` | `planned -> active` | `docs/UX_EXECUTION_TASKS.md` | 已核对干净工作区、`main...origin/main` 一致、现有 `app/index.html` / `app/app.js` / `app/styles.css` 入口位置，以及已完成 CRM 台账。 | 待提交 | UX 范围独立建账；下一步完成设置、顶部栏、导航、跟进、今日推进和达人表的视觉/行为基线盘点后，才激活 `UX-10-01`。 |
| 2026-09-13（America/New_York） | `UX-00-01` | `active -> done` | `docs/UX_EXECUTION_TASKS.md` | 使用隔离存储目录和独立访问密码启动 `http://localhost:4191/`；桌面截图确认现状为：三时区、品牌管理、刷新、JSON 导入导出、设置同列常驻，八个页面平铺导航，空数据默认达人库。未连接 IMAP、SMTP、AI 或真实数据。 | 待提交 | 基线完成；已全览台账，未重复已完成 CRM 能力。仅激活 `UX-10-01`，下一步将设置改为六类二级导航并保持原字段/接口兼容。 |
| 2026-09-13（America/New_York） | `UX-10-01` | `planned -> active` | `docs/UX_EXECUTION_TASKS.md` | 已确认设置页当前由 `renderSettingsPage()` 同时渲染 AI、品牌、邮箱、邮件安全、自动化、时区和录入候选项；HTML 结构和保存监听将作为兼容边界保留。 | 待提交 | 先建立设置页分区容器和状态导航，再逐项将既有模块映射到对应分区；不改变真实配置和邮件安全策略。 |
| 2026-09-13（America/New_York） | `UX-10-01` | `active -> active` | `app/index.html`、`app/app.js`、`app/styles.css`、`docs/UX_EXECUTION_TASKS.md` | 已在隔离存储的 `http://localhost:4191/` 核对六个分类分别只显示相关设置内容；现有表单 ID 与保存逻辑保持不变。`npm.cmd run check` 与 `git diff --check` 均通过。未读取正式资料，也未连接 IMAP、SMTP、AI 或线上存储。 | `cceb37e` | 设置页分区实现与基础回归已完成；提交后继续进行窄视口与未保存输入切换验证，满足验收后再关闭 `UX-10-01`。 |
| 2026-09-13（America/New_York） | `UX-10-01` | `active -> done` | `docs/UX_EXECUTION_TASKS.md` | Playwright 在隔离预览中填入“未保存品牌验证”，切换至 AI 与自动化后返回，输入仍保留；`480×900` 截图确认二级导航降为单列、无文字遮挡或白底残留。 | 待提交 | 设置页分区验收完成，未改变 API、真实连接或资料。已全览任务表，无重复项；仅激活 `UX-10-02`，下一步将审阅现有邮箱字段和测试入口后构建分步向导。 |
| 2026-09-13（America/New_York） | `UX-10-02` | `planned -> active` | `docs/UX_EXECUTION_TASKS.md` | 前置 `UX-10-01` 已完成；现有邮箱账户编辑、IMAP、SMTP、签名和测试接口将保持为兼容边界。 | 待提交 | 先盘点当前邮件设置 DOM、事件监听和测试入口；仅改变呈现与引导，不执行真实连接或发信。 |
| 2026-09-13（America/Los_Angeles） | `UX-10-02` | `active -> active` | `app/index.html`、`app/app.js`、`app/styles.css` | 已加入四步邮箱向导：账户与品牌、IMAP 收取、SMTP 发信、完成与测试；既有字段 ID、保存、测试与同步入口未改变。`npm.cmd run check` 与 `git diff --check` 通过。未读取正式资料，也未连接 IMAP、SMTP、AI 或线上存储。 | `8f63734` | 实现检查点可供测试仓库验证；下一门槛是隔离预览逐步切换、未保存输入保持及窄视口视觉验收，完成前保持 `active`，不启动 `UX-10-03`。 |
| 2026-09-13（America/Los_Angeles） | `UX-10-02` | `active -> done` | `app/app.js`、`docs/UX_EXECUTION_TASKS.md` | 隔离预览逐步核验：第 1 步不展示保存/测试/同步动作；第 2、3 步仅展示相应协议字段；第 4 步展示配置状态、测试入口与人工确认边界。修复完成页误隐藏的问题后，未保存账户名称在步骤往返后仍保留；`480×900` 截图无重叠、分类导航单列可读。`npm.cmd run check` 与 `git diff --check` 通过。 | `79885fa` | 邮箱向导验收完成，未连接真实服务或资料；已核对任务表无重叠，唯一激活 `UX-10-03`，下一步盘点 AI 设置的默认字段与低频连接字段。 |
| 2026-09-13（America/Los_Angeles） | `UX-10-03` | `planned -> active` | `docs/UX_EXECUTION_TASKS.md` | 前置设置分区与邮箱向导均已验收；AI 模型默认项、各功能分配与高级连接字段将保持现有存储和接口兼容。 | 待提交 | 先定位 AI 设置 DOM、渲染与保存边界；不调用真实模型，不变更既有 API 参数或配置值。 |
| 2026-09-13（America/Los_Angeles） | `UX-10-03` | `active -> done` | `app/index.html`、`app/app.js`、`app/styles.css` | 三套模型默认仅显示配置状态、当前模型、协议/Key 来源摘要；协议、API 地址、Key 与代理收进独立高级连接区，功能分配保持常驻。`npm.cmd run check`、`git diff --check` 通过。隔离预览验证高级区字段完整、未保存模型名跨分类切换仍保留；桌面与 `480×900` 截图无白底、遮挡或不可读控件。未保存、未检测网络、未调用真实 AI/邮箱服务。 | `3e4609e` | AI 设置精简验收完成，未改变 API 载荷、模型分配或连接语义。已全览任务表，无重复工作；仅激活 `UX-20-01`，下一步盘点首页默认页、空数据起点与现有导航切换边界。 |
| 2026-09-13（America/Los_Angeles） | `UX-20-01` | `planned -> active` | `docs/UX_EXECUTION_TASKS.md` | 前置 `UX-00-01` 已完成；将仅调整初始页面决策与无数据起点，不改变达人、品牌、导入或跟进数据。 | 待提交 | 先核对初始 active tab、可用业务数据判断、今日推进空状态及三个已有动作入口；不提前启动导航分组、顶部工具栏或跟进看板改造。 |
| 2026-09-13（America/Los_Angeles） | `UX-20-01` | `active -> done` | `app/app.js`、`app/styles.css`、`tools/today-action-center-regression-test.cjs`、`tools/fixtures/empty-workspace-state.json` | 初始加载统一落在今日推进；已有资料的隔离预览确认显示今日行动中心；完全空白的独立临时 SQLite 预览确认显示导入表格、新增达人、创建品牌三条入口。三入口分别复用既有表格导入、达人编辑器和品牌向导，不会直接写入资料。`npm.cmd run check`、`npm.cmd run test:today-action-center` 与 `git diff --check` 均通过；未连接真实 AI、IMAP、SMTP 或线上存储。 | `acd1585`、`7efa399` | 首页入口验收完成；已全览任务表，无重叠实现。仅激活 `UX-20-02`，下一步只处理资料/协作/分析三组导航与当前页高亮。 |
| 2026-09-13（America/Los_Angeles） | `UX-20-02` | `active -> done` | `app/app.js`、`app/styles.css`、`tools/today-action-center-regression-test.cjs` | 顶部业务入口重组为资料（达人库、待开发达人、产品库、资源库）、协作（今日推进、合作跟进）、分析（本周资源匹配、合作记录）；协作的两个日常入口获得优先级样式。保留全部既有 `data-tab` 键和切换处理，不改变页面路由、品牌筛选、数据或 API 契约。`npm.cmd run check`、`npm.cmd run test:today-action-center` 与 `git diff --check` 通过。隔离预览 `http://localhost:4194/` 的无真实外部服务会话中确认桌面分组可读，点击“合作跟进”正确切换页面且活动按钮、三组标签和优先入口均正确。 | `c87ac5c` | 导航分组验收完成；已全览任务表，无重复工作。仅激活 `UX-20-03`，下一步只收拢顶部工具栏，保留现有功能入口。 |
| 2026-09-13（America/Los_Angeles） | `UX-20-03` | `active -> done` | `app/index.html`、`app/app.js`、`app/styles.css`、`tools/today-action-center-regression-test.cjs` | 顶栏保留全局搜索、当前工作区、待处理数量、设置与两个优先时区；刷新资料、品牌管理、时区管理、JSON 导出/导入已进入“更多”菜单，且复用原有按钮 ID 与导入输入控件。额外时区以 `+N` 按钮展开。隔离存储预览 `http://localhost:4196/` 的可访问性快照确认紧凑首屏、两时区、`+1`、今日待处理与更多菜单入口；管理时区正确进入设置的偏好设置分区。`npm.cmd run check`、`npm.cmd run test:today-action-center` 与 `git diff --check` 均通过；未读取正式资料，未连接 AI、IMAP、SMTP 或线上存储。 | `1947abe` | 顶栏收拢验收完成；已全览固定任务表，前置任务满足且未重复既有 CRM 能力。仅激活 `UX-30-01`，下一步先盘点合作跟进页面的看板、Case、任务和邮件入口，再构建默认行动列表。 |
| 2026-09-13（America/Los_Angeles） | `UX-30-01` | `planned -> active` | `docs/UX_EXECUTION_TASKS.md` | 前置 `UX-00-01` 已完成；合作跟进的现有 Case、阶段、任务与邮件入口将作为兼容边界保留。 | 待提交 | 先确认当前看板渲染、筛选、主操作和窄视口行为；仅新增默认行动列表与切换入口，不改变邮件、AI、存储或自动阶段推进。 |

## 阶段完成记录模板

```text
时间：YYYY-MM-DD HH:mm（时区）
任务：UX-XX-XX
状态：planned/active/blocked/done -> 新状态
目标：本次最小改动解决的问题
改动：文件路径；关键行为变化
验证：执行命令、结果；人工验证步骤和结果
数据影响：是否迁移、是否触碰正式数据、回滚方式
提交：commit hash 或“待提交”
核对：已确认未重复完成；前置任务满足；下一门槛
```
